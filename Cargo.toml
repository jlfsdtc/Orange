[workspace]
members = [
    "crates/orange-core",
    "crates/orange-regex",
    "crates/orange-utils",
    "crates/orange-settings",
    "crates/orange-ui",
    "crates/orange-app",
]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/jlfsdtc/Orange"
rust-version = "1.90"

[workspace.dependencies]
# GUI
gpui = "0.2"

# Performance critical
hyperscan = "0.3"
roaring = "0.10"
mimalloc = { version = "0.1", default-features = false }
xxhash-rust = { version = "0.8", features = ["xxh3"] }

# Parallelism
rayon = "1.8"
flume = "0.11"

# Async / IO
tokio = { version = "1", features = ["rt-multi-thread", "fs", "io-util"] }

# File watching
notify = "8"

# Encoding
encoding_rs = "0.8"
simdutf8 = "0.1"
chardetng = "0.1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1"

# Error handling
anyhow = "1"
thiserror = "2"

# CLI
clap = { version = "4", features = ["derive"] }

# Testing
criterion = { version = "0.7", features = ["html_reports"] }

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = "symbols"
